Як було
- `src/pages/Activity.tsx` мав кілька неочевидних branch-path у пошуку за `projectMap`, обрізанні списку на `PAGE_SIZE + 1`, realtime cleanup та anomaly-логіці.
- `src/pages/__tests__/Activity.test.tsx` уже добре покривав базові сценарії, але не бив по цих окремих негативних і fallback-гілках.

Що зроблено
- Додано тест на пошук за назвою проєкту через `projectMap`.
- Додано тест, що при `PAGE_SIZE + 1` відображаються лише перші 50 логів і останній рядок відкидається.
- Додано тест на fallback до скороченого `project_id`, коли мапа проєктів не містить відповідного запису.
- Додано anomaly-тест для сценарію без успішних операцій за останні 2 години.
- Додано deterministic cleanup-тест для realtime channel на `unmount`.

Що покращило
- Розширено branch coverage для `Activity` у зонах, де раніше лишалися локальні UI та async-гілки.
- Тести стали точніше перевіряти fallback і cleanup-поведінку без змін production-коду.
- Batch V лишився повністю тестовим і ізольованим.
