# Зміни від 2026-04-23: Застосування міграції scan mode та ремонт migration history

## Як було
- `supabase db push` падав на першій історичній міграції через дубль policy (`Users read own profile`).
- Причина: у remote БД об'єкти вже існували, але журнал міграцій був неповний.
- Цільова міграція `20260423191500_add_scan_mode_columns.sql` не була застосована.

## Що зроблено
- Виконано `supabase migration repair --status applied` для історичних версій (до `20260422`) для синхронізації remote migration history.
- Повторно виконано `supabase db push` і застосовано:
  - `20260423191500_add_scan_mode_columns.sql`
  - `20260423_full_rbac_and_agent_fix.sql` (частково з NOTICE по вже існуючих об'єктах)
- Перевірено `supabase migration list`: версія `20260423191500` присутня в Local і Remote (applied).

## Що покращило/виправило/додало
- Цільова міграція scan mode фактично виконана в remote БД.
- Знято первинний блокер застосування міграцій (дублікати політик на старті історії).
- Зафіксовано залишковий технічний борг migration history: версія `20260423` має нестабільний стан у журналі й потребує окремого cleanup-кроку перед повним `db push` без попереджень.
