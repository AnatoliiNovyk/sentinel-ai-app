# Changelog — Batch-89

## Що було
- `Projects.tsx` показував всі проєкти у сітці без можливості пошуку, фільтрації чи сортування.
- При великій кількості проєктів — важко знайти потрібний.

## Що зроблено
- Додано `search` state + текстовий input з іконкою Search та кнопкою "×" для очищення.
- Додано `envFilter` state + кнопки-чіпи для фільтрації за середовищем: All / External / Cloud / Internal / IaC.
- Додано `sort` state + `<select>` для сортування: Newest first / Oldest first / Risk ↓ / Risk ↑ / Name A–Z.
- `visible` масив обчислюється через `useMemo` — фільтрація + сортування без зайвих рендерів.
- При відсутності результатів — empty state з кнопкою "Clear filters".
- Лічильник "X of N" відображається коли фільтри активні.

## Що покращило
- Швидкий пошук проєктів за назвою, доменом або описом.
- Миттєва фільтрація за типом середовища.
- Гнучке сортування за датою, ризиком або алфавітом.
- Commit: `Batch-89: Projects page — search bar + env filter + sort`
